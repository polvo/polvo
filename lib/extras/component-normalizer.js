// Generated by CoffeeScript 1.6.3
var config, dirs, fs, fsu, path;

fs = require('fs');

path = require('path');

fsu = require('fs-util');

dirs = require('../utils/dirs');

config = require('../utils/config');

module.exports = function() {
  var components, manifest_path, manifests, name, _i, _len, _results;
  components = path.join(dirs.pwd, 'components');
  if (!fs.existsSync(components)) {
    return;
  }
  manifests = fsu.find(components, /component\.json/);
  _results = [];
  for (_i = 0, _len = manifests.length; _i < _len; _i++) {
    manifest_path = manifests[_i];
    name = require(manifest_path).name;
    _results.push(config.alias[name] = dirs.relative(path.dirname(manifest_path)));
  }
  return _results;
};

/*
//@ sourceMappingURL=component-normalizer.map
*/
