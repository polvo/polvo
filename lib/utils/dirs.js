// Generated by CoffeeScript 1.6.3
var argv, cli, debug, error, fs, info, log, path, pwd, warn, _ref;

path = require('path');

fs = require('fs');

cli = require('../cli');

_ref = require('./logger')('utils/dirs'), error = _ref.error, warn = _ref.warn, info = _ref.info, debug = _ref.debug, log = _ref.log;

argv = cli.argv();

exports.root = path.join(__dirname, '..', '..');

if (argv.base != null) {
  if (!fs.existsSync((pwd = path.resolve(argv.base)))) {
    error('Dir informed with [--base] option doesn\'t exist ~>', argv.base);
    pwd = null;
  }
} else {
  pwd = path.resolve('.');
}

exports.pwd = pwd;

exports.relative = function(filepath) {
  return path.relative(exports.pwd, filepath);
};

/*
//@ sourceMappingURL=dirs.map
*/
